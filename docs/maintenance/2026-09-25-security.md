# Security maintenance — 2026-09-25

Frontend dependency updates clear the npm findings. The desktop size formatter now preserves the two-decimal output required by its existing Windows test contract.

## Verification

Frontend tests/build and npm audit passed. A focused formatter probe passed for bytes, KB, MB and GB. Windows basic tests now pass. CI previously omitted PyInstaller; the development requirements now include the packaging tool, and artifact upload uses the executable path emitted by build.py. Windows tests and executable packaging now pass. The full 18-package Windows build lock resolves successfully and pip-audit reports zero known vulnerabilities. CI installs that lock reproducibly.

These are source and dependency checks. A merged commit alone does not establish that an installed or hosted runtime has been updated. Deployment and local uptake receipts belong in the estate maintenance report.

## Reproduction and rollback

Install from the committed lockfile using the repository's documented runtime, then rerun its tests/build and audit every nested dependency root. Preserve local work and back up persistent data before restarting a deployed app. Roll back with the previous verified release artifact or a focused revert of this maintenance change; do not reset a dirty checkout.

Maintenance PR: https://github.com/tomstetson/FieldTuner-Desktop/pull/1
